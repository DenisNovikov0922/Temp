<?php
if ( ! defined( 'ABSPATH' ) ) {
	exit; // Exit if accessed directly
}

global $current_user;

$notification_types['ticket_rating']   = __('Ticket Rating','wpsc-sf');
$notification_types['ticket_feedback'] = __('Ticket Feedback','wpsc-sf');
